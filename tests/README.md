# Tests

No automated tests exist yet in Phase 1 (Foundation) beyond CI's lint /
type-check / build gate. This folder is reserved for future test suites
(e.g. Vitest/Jest for unit tests, Playwright for e2e) as business features
are added in later phases.
